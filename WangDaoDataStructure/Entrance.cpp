#include"2.2SqList.h"
#include"Utils/DataUtils.h"
int main() {
	/*SqList* sql = InitSqList();
	for (int i = 0; i < 25; i++)
	{
		sql->data[i] = i;
	}
	sql->data[25] = -1;
	for (int i = 26; i < MaxSize; i++)
	{
		sql->data[i] = i;
	}
	OrderReversed(sql);*/
	int a = 1, b = 2;
	ExchangeByBit(&a, &b);
	return 0;
}