 #include"2.2SqList.h"
 #include"Tree.h"
int main() {
	TreeNode* head = InitTree();
	/*SqList* sql = InitSqList();
	for (int i = 0; i < 25; i++)
	{
		sql->data[i] = i;
	}
	sql->data[25] = 25;
	for (int i = 26; i < MaxSize; i++)
	{
		sql->data[i] = i;
	}
	sql->length = 50;
	LeftMovePPosition(sql, 15);
	return 0;*/
}